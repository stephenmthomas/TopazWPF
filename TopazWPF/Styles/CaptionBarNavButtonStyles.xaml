<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 
        Caption Bar Navigation Button Styles
        
        Usage in caption bar navigation:
        <Button Content="Home" Style="{StaticResource CaptionBarNavButtonStyle}" Tag="0" Click="NavButton_Click"/>
        <Button Content="Settings" Style="{StaticResource CaptionBarNavButtonActiveStyle}" Tag="1" Click="NavButton_Click"/>
    -->

    <!-- Base Caption Bar Navigation Button Style -->
    <Style x:Key="CaptionBarNavButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.Text.Tertiary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Margin" Value="12,0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.Primary.Hover}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.Primary.Light}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Active/Selected Caption Bar Navigation Button Style -->
    <Style x:Key="CaptionBarNavButtonActiveStyle" TargetType="Button" BasedOn="{StaticResource CaptionBarNavButtonStyle}">
        <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.Primary.Base}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

</ResourceDictionary>
