<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:themes="clr-namespace:TopazWPF.Themes;assembly=TopazWPF">

    <!-- ═══════════════════════════════════════════════════════════════
         BUTTON STYLES — Themed with Variants
         
         Variants:
           (default)                Dark surface button
           PrimaryButtonStyle       Accent colored (purple/blue)
           OutlineButtonStyle       Transparent with border
           GhostButtonStyle         Transparent no border
           DangerButtonStyle        Red destructive action
         ═══════════════════════════════════════════════════════════════ -->

    <!-- ── Default Button (implicit style) ─────────────────────────── -->
    <Style TargetType="{x:Type Button}">
        <Setter Property="Foreground"       Value="{Binding Path=(themes:Theme.Foreground), RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Background"       Value="{Binding Path=(themes:Theme.Background), RelativeSource={RelativeSource Self}}"/>
        <Setter Property="BorderBrush"      Value="{Binding Path=(themes:Theme.BorderBrush), RelativeSource={RelativeSource Self}}"/>
        <Setter Property="BorderThickness"  Value="{DynamicResource Theme.BorderThickness.Button}"/>
        <Setter Property="Padding"          Value="15,9"/>
        <Setter Property="FontSize"         Value="{DynamicResource Theme.Font.Size.Body}"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource Theme.Radius.Large}"
                        SnapsToDevicePixels="True">

                        <ContentPresenter
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="{TemplateBinding Padding}"
                        RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" 
                                Value="{Binding Path=(themes:Theme.HoverBackground), RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Setter TargetName="Root" Property="BorderBrush" 
                                Value="{Binding Path=(themes:Theme.HoverBorderBrush), RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" 
                                Value="{Binding Path=(themes:Theme.PressedBackground), RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Setter TargetName="Root" Property="BorderBrush" 
                                Value="{Binding Path=(themes:Theme.PressedBorderBrush), RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" 
                                Value="{DynamicResource Theme.Brush.Primary.Base}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <!-- ── PRIMARY BUTTON ──────────────────────────────────── -->
    <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground"       Value="{DynamicResource Theme.Brush.White}"/>
        <Setter Property="Background"       Value="{DynamicResource Theme.Brush.Primary.Base}"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource Theme.Brush.Primary.Base}"/>
        <Setter Property="BorderThickness"  Value="{DynamicResource Theme.BorderThickness.Button}"/>
        <Setter Property="Padding"          Value="15,9"/>
        <Setter Property="FontSize"         Value="{DynamicResource Theme.Font.Size.Body}"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource Theme.Radius.Large}"
                            SnapsToDevicePixels="True">

                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="{TemplateBinding Padding}"
                            RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                    Value="{DynamicResource Theme.Brush.Primary.Hover}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                    Value="{DynamicResource Theme.Brush.Primary.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                    Value="{DynamicResource Theme.Brush.Primary.Pressed}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                    Value="{DynamicResource Theme.Brush.Primary.Pressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background"
                                    Value="{DynamicResource Theme.Brush.Surface.Dark}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                    Value="{DynamicResource Theme.Brush.Border.Subtle}"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Theme.Brush.Text.Muted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── SECONDARY BUTTON ────────────────────────────────── -->
    <Style x:Key="SecondaryButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground"       Value="{DynamicResource Theme.Brush.White}"/>
        <Setter Property="Background"       Value="{DynamicResource Theme.Brush.Secondary.Base}"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource Theme.Brush.Secondary.Base}"/>
        <Setter Property="BorderThickness"  Value="{DynamicResource Theme.BorderThickness.Button}"/>
        <Setter Property="Padding"          Value="15,9"/>
        <Setter Property="FontSize"         Value="{DynamicResource Theme.Font.Size.Body}"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource Theme.Radius.Large}"
                        SnapsToDevicePixels="True">

                        <ContentPresenter
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="{TemplateBinding Padding}"
                        RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Secondary.Hover}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Secondary.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Secondary.Pressed}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Secondary.Pressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Surface.Dark}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Border.Subtle}"/>
                            <Setter Property="Foreground"
                                Value="{DynamicResource Theme.Brush.Text.Muted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── TERTIARY BUTTON ─────────────────────────────────── -->
    <Style x:Key="TertiaryButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground"       Value="{DynamicResource Theme.Brush.White}"/>
        <Setter Property="Background"       Value="{DynamicResource Theme.Brush.Tertiary.Base}"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource Theme.Brush.Tertiary.Base}"/>
        <Setter Property="BorderThickness"  Value="{DynamicResource Theme.BorderThickness.Button}"/>
        <Setter Property="Padding"          Value="15,9"/>
        <Setter Property="FontSize"         Value="{DynamicResource Theme.Font.Size.Body}"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource Theme.Radius.Large}"
                        SnapsToDevicePixels="True">

                        <ContentPresenter
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="{TemplateBinding Padding}"
                        RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Tertiary.Hover}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Tertiary.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Tertiary.Pressed}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Tertiary.Pressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Surface.Dark}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Border.Subtle}"/>
                            <Setter Property="Foreground"
                                Value="{DynamicResource Theme.Brush.Text.Muted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── SUCCESS BUTTON ──────────────────────────────────── -->
    <Style x:Key="SuccessButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground"       Value="{DynamicResource Theme.Brush.White}"/>
        <Setter Property="Background"       Value="{DynamicResource Theme.Brush.Success.Base}"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource Theme.Brush.Success.Base}"/>
        <Setter Property="BorderThickness"  Value="{DynamicResource Theme.BorderThickness.Button}"/>
        <Setter Property="Padding"          Value="15,9"/>
        <Setter Property="FontSize"         Value="{DynamicResource Theme.Font.Size.Body}"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource Theme.Radius.Large}"
                        SnapsToDevicePixels="True">

                        <ContentPresenter
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="{TemplateBinding Padding}"
                        RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Success.Hover}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Success.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Success.Pressed}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Success.Pressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Surface.Dark}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Border.Subtle}"/>
                            <Setter Property="Foreground"
                                Value="{DynamicResource Theme.Brush.Text.Muted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── WARNING BUTTON ──────────────────────────────────── -->
    <Style x:Key="WarningButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground"       Value="{DynamicResource Theme.Brush.White}"/>
        <Setter Property="Background"       Value="{DynamicResource Theme.Brush.Warning.Base}"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource Theme.Brush.Warning.Base}"/>
        <Setter Property="BorderThickness"  Value="{DynamicResource Theme.BorderThickness.Button}"/>
        <Setter Property="Padding"          Value="15,9"/>
        <Setter Property="FontSize"         Value="{DynamicResource Theme.Font.Size.Body}"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource Theme.Radius.Large}"
                        SnapsToDevicePixels="True">

                        <ContentPresenter
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="{TemplateBinding Padding}"
                        RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Warning.Hover}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Warning.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Warning.Pressed}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Warning.Pressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource Theme.Brush.Surface.Dark}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                Value="{DynamicResource Theme.Brush.Border.Subtle}"/>
                            <Setter Property="Foreground"
                                Value="{DynamicResource Theme.Brush.Text.Muted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── DANGER BUTTON ─────────────────────────────────────────────── -->
    <Style x:Key="DangerButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground"       Value="{DynamicResource Theme.Brush.White}"/>
        <Setter Property="Background"       Value="{DynamicResource Theme.Brush.Error.Base}"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource Theme.Brush.Error.Base}"/>
        <Setter Property="BorderThickness"  Value="{DynamicResource Theme.BorderThickness.Default}"/>
        <Setter Property="Padding"          Value="15,9"/>
        <Setter Property="FontSize"         Value="{DynamicResource Theme.Font.Size.Body}"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource Theme.Radius.Large}"
                            SnapsToDevicePixels="True">

                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="{TemplateBinding Padding}"
                            RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                    Value="{DynamicResource Theme.Brush.Error.Hover}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                    Value="{DynamicResource Theme.Brush.Error.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                    Value="{DynamicResource Theme.Brush.Error.Pressed}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                    Value="{DynamicResource Theme.Brush.Error.Pressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background"
                                    Value="{DynamicResource Theme.Brush.Surface.Dark}"/>
                            <Setter TargetName="Root" Property="BorderBrush"
                                    Value="{DynamicResource Theme.Brush.Border.Subtle}"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Theme.Brush.Text.Muted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── OUTLINE BUTTON ───────────────────────────────────────────── -->
    <Style x:Key="OutlineButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground"       Value="{DynamicResource Theme.Brush.Text.Primary}"/>
        <Setter Property="Background"       Value="Transparent"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource Theme.Brush.Border.Default}"/>
        <Setter Property="BorderThickness"  Value="{DynamicResource Theme.BorderThickness.Default}"/>
        <Setter Property="Padding"          Value="15,9"/>
        <Setter Property="FontSize"         Value="{DynamicResource Theme.Font.Size.Body}"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource Theme.Radius.Large}"
                            SnapsToDevicePixels="True">

                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="{TemplateBinding Padding}"
                            RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Theme.Brush.FocusGlow30}"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border.Strong}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Theme.Brush.FocusGlow50}"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border.Strong}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border.Subtle}"/>
                            <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.Text.Muted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── GHOST BUTTON ──────────────────────────────────────────────── -->
    <Style x:Key="GhostButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground"       Value="{DynamicResource Theme.Brush.Text.Secondary}"/>
        <Setter Property="Background"       Value="Transparent"/>
        <Setter Property="BorderBrush"      Value="Transparent"/>
        <Setter Property="BorderThickness"  Value="0"/>
        <Setter Property="Padding"          Value="15,9"/>
        <Setter Property="FontSize"         Value="{DynamicResource Theme.Font.Size.Body}"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource Theme.Radius.Large}"
                            SnapsToDevicePixels="True">

                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="{TemplateBinding Padding}"
                            RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Theme.Brush.Text.Primary}"/>
                            <Setter TargetName="Root" Property="Background"
                                    Value="{DynamicResource Theme.Brush.FocusGlow30}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                    Value="{DynamicResource Theme.Brush.FocusGlow50}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Theme.Brush.Text.Muted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



</ResourceDictionary>
